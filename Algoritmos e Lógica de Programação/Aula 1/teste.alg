Algoritmo "mediaDosPesos"
    Var peso1: real
        peso2: real
        soma: real
        media: real

Inicio 
    escreval ("Insira o primeiro peso:")
    leia(peso1)
    escreval ("Insira o segundo peso:")
    leia(peso2)
    soma <- peso1 + peso2
    media <- soma / 2
    escreval(media)

Fim